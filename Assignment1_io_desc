##################Input Output Transformation###########################


1) First reads data from delimited text files into a PDI transformation.
2) Then load it into text output file with CSV extension.
3) Then run the script. It gives me same number of rows present into input CSV as below,

